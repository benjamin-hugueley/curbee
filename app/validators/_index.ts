import { Appointment } from './appointment';
import { Customer } from './customer';
import { Location } from './location';
import { Vehicle } from './vehicle';

/**
 * Provides a namespace for all validation functions.
 * This allows for structured and organized access to different validators.
 */
const validators = {
    Appointment,
    Customer,
    Location,
    Vehicle
};

export { validators };