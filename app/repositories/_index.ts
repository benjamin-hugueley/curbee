import { appointments } from './appointments.js';

/**
 * Provides a namespace for all data repositories.
 * This allows for structured and organized access to different repositories.
 */
const repositories = {
  appointments
};

export { repositories };